# Nemotron ASR Resources

This directory is reserved for optional local ASR model payloads.

The generated model files are large and are not committed to the source repository. Release builds may bundle these resources, and local development environments can place them here when offline ASR is needed.

Without these files, RenJistroly still builds, but local Nemotron ASR features may be unavailable.
